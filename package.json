{
  "name": "coreplus",
  "version": "3.0.0",
  "private": true,
  "description": "CorePlus — extends multi-period filtering beyond the default and adds sequential bulk-crediting features to the Coretax portal.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "zip": "cd dist && zip -r ../coreplus-v$npm_package_version.zip . -x '*.DS_Store'"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.7.0",
    "@types/chrome": "^0.0.270",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.108.2"
  }
}
